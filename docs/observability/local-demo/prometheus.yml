global:
  scrape_interval: 10s
  evaluation_interval: 10s

rule_files:
  - /etc/prometheus/rules/agent-gateway.yaml

scrape_configs:
  - job_name: agent-gateway-otel-collector
    static_configs:
      - targets:
          - otel-collector:9464
